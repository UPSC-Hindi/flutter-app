import 'package:flutter/material.dart';

class DevicesLogin extends StatelessWidget {
  const DevicesLogin({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DevicesLogin'),
      ),
      body: Center(
        child: Text('DevicesLogin'),
      ),
    );
  }
}
