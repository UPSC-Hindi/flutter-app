import 'package:flutter/material.dart';

class mocktestscreen extends StatelessWidget {
  const mocktestscreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text('mocktestscreen'),
      ),
    );
  }
}
